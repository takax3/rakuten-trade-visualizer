# AGENTS.md

このリポジトリで作業するエージェント向けの運用ルールです。

## 作業方針

- 既存の構成と実装スタイルを優先し、不要な大規模リファクタリングは避ける。
- 楽天証券CSVの解釈、損益計算、チャート表示の仕様を変える場合は、対応するテストを追加または更新する。
- 手数料、金利、税金などを損益計算に含める変更は、明示的な要件がある場合だけ行う。
- ユーザー向け表示は日本語を基本とし、数値は既存画面に合わせて桁区切りで表示する。
- ローカル検証は原則として `uv run pytest` を使う。

## コミットメッセージ

コミットメッセージは以下のprefix形式にする。

```text
<prefix>: <summary>
```

利用するprefixは次を基本とする。

- `feat`: ユーザー向け機能の追加や変更
- `fix`: バグ修正
- `docs`: ドキュメントのみの変更
- `test`: テストの追加や修正
- `refactor`: 振る舞いを変えない整理
- `style`: 表示、CSS、フォーマットなどの変更
- `chore`: 開発環境、依存関係、雑務

例:

```text
feat: add realized pnl to execution table
docs: add agent workflow guide
```

## 注意点

- `app/models.py` のデータ構造はチャート、パーサ、テンプレートで共有されるため、変更時は影響範囲を確認する。
- 約定一覧の表示とMarkdownコピー内容がずれないよう、表の表示内容を基準にする。
- Yahoo Finance取得は外部サービスに依存するため、テストではネットワーク前提の検証を避ける。
